# Frontend Environment Variables (VITE_*)
# These are embedded into the frontend code at build time
# IMPORTANT: Never put API keys with VITE_ prefix - they will be exposed in frontend!

# Enable proxy mode (tells frontend to use backend proxy for API calls)
# Set to "true" to enable, any other value or undefined to disable
VITE_USE_PROXY=true

# Optional: Display model name in UI
VITE_MODEL=gemini-2.5-flash

# Optional: Display base URL in UI (for custom endpoints)
# VITE_BASE_URL=https://generativelanguage.googleapis.com

# Backend Environment Variables (GEMINI_*)
# These are ONLY available on the backend/server-side
# Configure these in your deployment platform (Vercel, etc.)

# Required: Your actual Gemini API key (backend only, never exposed to frontend)
# GEMINI_API_KEY=your_api_key_here

# Optional: Model to use for API calls (backend)
# GEMINI_MODEL=gemini-2.5-flash

# Optional: Custom API endpoint (backend)
# GEMINI_BASE_URL=https://generativelanguage.googleapis.com
